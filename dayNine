# Enter your code here. Read input from STDIN. Print output to STDOUT
import sys

def fact(N):
    if N == 0:
        N += 1
    else:
        N = N * fact(N-1)
    return N

N = int(raw_input().strip())
print fact(N),